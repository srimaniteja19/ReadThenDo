export const ANTI_HABITS_JSON = `"antiHabits": [
    {
      "name": "Short stop-doing behavior (max 6 words)",
      "why": "One sentence: why eliminating this supports the book's framework."
    }
  ],`;

export const ANTI_HABITS_INSTRUCTION = `Also extract exactly 3 anti-habits — behaviors the reader should STOP doing based on the book. Frame as elimination, not addition.`;
